# ===========================================
# x402Guard Environment Variables
# Copy this to .env and fill in your values
#
# SECURITY WARNING: Never commit .env files!
# Generate secrets with: openssl rand -hex 32
# ===========================================

# --- Rust Proxy ---
PROXY_HOST=0.0.0.0
PROXY_PORT=3402
# SECURITY: Use 'warn' in production. NEVER use 'debug' or 'trace' for DeFi.
# Development: info,x402_guard_proxy=debug
# Production:  warn
RUST_LOG=info,x402_guard_proxy=debug

# --- Management API ---
# SECURITY: Required for production. If unset, management API is DENIED (fail-closed).
# Generate with: openssl rand -hex 32
MANAGEMENT_API_KEY=

# --- Supabase ---
SUPABASE_URL=http://localhost:54321
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
DATABASE_URL=postgresql://postgres:postgres@localhost:54322/postgres

# --- Upstash Redis ---
UPSTASH_REDIS_URL=redis://localhost:6379
UPSTASH_REDIS_TOKEN=

# --- Docker Compose Overrides ---
# When running inside docker compose, use service names instead of localhost:
# DATABASE_URL=postgresql://postgres:postgres@postgres:5432/postgres
# UPSTASH_REDIS_URL=redis://redis:6379

# --- Chain RPCs ---
BASE_SEPOLIA_RPC_URL=https://sepolia.base.org
BASE_MAINNET_RPC_URL=https://mainnet.base.org
SOLANA_RPC_URL=https://api.devnet.solana.com
SOLANA_PROGRAM_ID=
SOLANA_USDC_MINT=

# --- Dashboard (Next.js) ---
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
NEXT_PUBLIC_PROXY_URL=http://localhost:3402
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# --- CORS ---
# Comma-separated allowed origins. Defaults to http://localhost:3000.
# ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com
