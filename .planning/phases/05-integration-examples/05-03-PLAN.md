---
phase: 05-integration-examples
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/virtuals/pyproject.toml
  - examples/virtuals/src/x402guard_game/__init__.py
  - examples/virtuals/src/x402guard_game/types.py
  - examples/virtuals/src/x402guard_game/errors.py
  - examples/virtuals/src/x402guard_game/client.py
  - examples/virtuals/src/x402guard_game/functions.py
  - examples/virtuals/tests/unit/test_client.py
  - examples/virtuals/tests/unit/test_errors.py
  - examples/virtuals/tests/unit/test_functions.py
  - examples/virtuals/tests/integration/__init__.py
  - examples/virtuals/tests/integration/test_proxy.py
  - examples/virtuals/demo/demo_worker.py
  - examples/virtuals/demo/demo_agent.py
  - examples/virtuals/README.md
autonomous: true
requirements: [FR-8.2, FR-8.4]

must_haves:
  truths:
    - "X402GuardClient can make HTTP calls to all proxy endpoints with typed request/response dataclasses"
    - "GuardrailViolationError is parsed from 403 responses with rule_type, limit, actual values"
    - "GameFunction wrappers (make_guarded_payment, query_solana_vault) execute via GAME SDK interface"
    - "demo_worker.py runs the full flow without VIRTUALS_API_KEY (direct function calls)"
    - "demo_agent.py runs the full agentic flow with VIRTUALS_API_KEY"
    - "README includes prerequisites, two demo modes, config reference, architecture diagram, troubleshooting"
    - "Integration tests against running proxy are gated by X402GUARD_INTEGRATION env var and verify real HTTP round-trips"
  artifacts:
    - path: "examples/virtuals/src/x402guard_game/types.py"
      provides: "Frozen dataclasses mirroring Rust proxy models"
      contains: "Agent, GuardrailRule, RuleType, SessionKey, ProxyRequest, ProxyResponse"
    - path: "examples/virtuals/src/x402guard_game/client.py"
      provides: "X402GuardClient async HTTP wrapper with all proxy endpoints"
      exports: ["X402GuardClient", "X402GuardConfig"]
    - path: "examples/virtuals/src/x402guard_game/errors.py"
      provides: "Typed exception hierarchy"
      exports: ["X402GuardError", "GuardrailViolationError", "ProxyUnreachableError"]
    - path: "examples/virtuals/src/x402guard_game/functions.py"
      provides: "GAME SDK GameFunction factories for x402Guard operations"
      exports: ["make_guarded_payment", "query_solana_vault", "guarded_payment_fn", "solana_vault_fn", "defi_worker"]
    - path: "examples/virtuals/pyproject.toml"
      provides: "Python package definition with hatchling"
      contains: "x402guard-game-plugin"
    - path: "examples/virtuals/tests/integration/test_proxy.py"
      provides: "Integration tests against live proxy (gated by X402GUARD_INTEGRATION)"
  key_links:
    - from: "examples/virtuals/src/x402guard_game/functions.py"
      to: "examples/virtuals/src/x402guard_game/client.py"
      via: "imports X402GuardClient for proxy calls"
      pattern: "from.*client.*import.*X402GuardClient"
    - from: "examples/virtuals/src/x402guard_game/functions.py"
      to: "game_sdk"
      via: "uses Function, Argument, FunctionResultStatus types"
      pattern: "from game_sdk"
    - from: "examples/virtuals/src/x402guard_game/client.py"
      to: "examples/virtuals/src/x402guard_game/errors.py"
      via: "throws typed exceptions on non-2xx responses"
      pattern: "GuardrailViolationError|ProxyUnreachableError"
---

<objective>
Build the Virtuals Protocol GAME plugin (`x402guard-game-plugin`) in Python that wraps x402Guard proxy as GAME SDK functions, plus two demo modes (worker-only and full agent) and comprehensive README.

Purpose: Demonstrates how Virtuals Protocol agents integrate x402Guard for guarded DeFi payments. This is the Python integration example (MUST priority FR-8.2). Fully independent of the TypeScript core package -- runs in parallel with Plan 01.

Output: `examples/virtuals/` package with X402GuardClient (httpx), GAME SDK function wrappers, two demo scripts, unit tests, and README.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-integration-examples/05-RESEARCH.md

<!-- Key proxy API types the Python SDK must mirror -->
<interfaces>
From proxy/src/models/agent.rs:
  Agent { id: Uuid, name: String, owner_address: String, created_at: DateTime, is_active: bool }

From proxy/src/models/guardrail.rs:
  GuardrailRule { id: Uuid, agent_id: Uuid, rule_type: RuleType, is_active: bool }
  RuleType (tagged enum, serde tag="type", content="params"):
    MaxSpendPerTx { limit: u64 }
    MaxSpendPerDay { limit: u64 }
    AllowedContracts { addresses: Vec&lt;String&gt; }
    MaxLeverage { max: u32 }
    MaxSlippage { bps: u32 }

From proxy/src/models/session_key.rs:
  SessionKey { id: Uuid, agent_id: Uuid, public_key: String, max_spend: u64, spent: u64, allowed_contracts: Vec&lt;String&gt;, expires_at: DateTime, is_revoked: bool, created_at: DateTime }

From proxy/src/handlers/proxy.rs (camelCase serde):
  ProxyRequest { targetUrl: String, x402Payment: String, x402Requirements: String, agentId?: String, sessionKeyId?: String }
  ProxyResponse { success: bool, txHash?: String, message: String, data?: any }
  SolanaProxyRequest { targetUrl: String, network: String, vaultOwner: String, amount: number, destinationProgram?: String, x402Payment: String }
  SolanaProxyResponse { success: bool, message: String, vaultPda?: String, remainingDailyCapacity?: number, data?: any }

From game_sdk (verified from github.com/game-by-virtuals/game-python):
  Function(fn_name, fn_description, args: list[Argument], executable: Callable)
  Argument(name, description)
  FunctionResultStatus (enum: DONE, FAILED)
  WorkerConfig(id, worker_description, get_state_fn, action_space: list[Function])
  Agent(api_key, name, agent_goal, agent_description, get_agent_state_fn, workers: list[WorkerConfig])

API routes (from proxy/src/router.rs):
  GET  /health
  POST /api/v1/agents
  GET  /api/v1/agents/:id
  POST /api/v1/agents/:id/rules
  GET  /api/v1/agents/:id/rules
  PUT  /api/v1/agents/:id/rules/:rule_id
  DELETE /api/v1/agents/:id/rules/:rule_id
  POST /api/v1/agents/:id/session-keys
  POST /api/v1/agents/:id/revoke-all
  POST /api/v1/proxy
  POST /api/v1/proxy/solana
  GET  /api/v1/solana/vault/:owner
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Python package scaffold with types, errors, and HTTP client</name>
  <files>
    examples/virtuals/pyproject.toml
    examples/virtuals/src/x402guard_game/__init__.py
    examples/virtuals/src/x402guard_game/types.py
    examples/virtuals/src/x402guard_game/errors.py
    examples/virtuals/src/x402guard_game/client.py
    examples/virtuals/tests/__init__.py
    examples/virtuals/tests/unit/__init__.py
    examples/virtuals/tests/unit/test_client.py
    examples/virtuals/tests/unit/test_errors.py
    examples/virtuals/tests/integration/__init__.py
    examples/virtuals/tests/integration/test_proxy.py
  </files>
  <action>
Create `examples/virtuals/` package as `x402guard-game-plugin`:

1. **pyproject.toml**: Use hatchling build backend. Package name `x402guard-game-plugin`, version 0.1.0, requires-python >=3.11. Dependencies: `game_sdk`, `httpx>=0.28`. Optional dev deps: `pytest>=8.0`, `pytest-asyncio>=0.24`, `pytest-httpx>=0.35`, `mypy>=1.0`. pytest.ini_options: asyncio_mode="auto", testpaths=["tests"]. mypy: strict=true, python_version="3.11". Hatch build sources: `["src"]`.

2. **src/x402guard_game/types.py**: Frozen dataclasses mirroring Rust proxy models exactly:
   - `@dataclass(frozen=True)` for ALL types (project immutability rule)
   - `Agent(id: str, name: str, owner_address: str, created_at: str, is_active: bool)`
   - `RuleType` as a dataclass with `type: str` (PascalCase: "MaxSpendPerTx", "MaxSpendPerDay", etc.) and `params: dict[str, Any]` -- mirrors Rust serde `tag="type" content="params"` format
   - `GuardrailRule(id: str, agent_id: str, rule_type: RuleType, is_active: bool)`
   - `SessionKey(id: str, agent_id: str, public_key: str, max_spend: int, spent: int, allowed_contracts: list[str], expires_at: str, is_revoked: bool, created_at: str)`
   - `ProxyRequest(target_url: str, x402_payment: str, x402_requirements: str, agent_id: str | None = None, session_key_id: str | None = None)` with `to_dict()` method that converts to camelCase keys matching Rust serde
   - `ProxyResponse(success: bool, message: str, tx_hash: str | None = None, data: dict | None = None)` with `@classmethod from_dict(cls, d: dict)` that converts from camelCase
   - `SolanaProxyRequest(target_url: str, network: str, vault_owner: str, amount: int, x402_payment: str, destination_program: str | None = None)` with `to_dict()`
   - `SolanaProxyResponse(success: bool, message: str, vault_pda: str | None = None, remaining_daily_capacity: int | None = None, data: dict | None = None)` with `from_dict()`
   - `CreateAgentRequest(name: str, owner_address: str)`
   - `CreateRuleRequest(rule_type: RuleType)` with `to_dict()` producing `{"rule_type": {"type": "...", "params": {...}}}`
   - `RevokeAllRequest(owner_address: str, chain_id: int | None = None, eoa_nonce_hint: int | None = None)`
   - `X402GuardConfig(proxy_url: str, agent_id: str | None = None, log_level: str = "INFO", max_retries: int = 3, retry_base_ms: int = 1000)` with validation in `__post_init__`
   - Helper factory functions: `max_spend_per_tx(limit: int) -> RuleType`, `max_spend_per_day(limit: int) -> RuleType`, `allowed_contracts(addresses: list[str]) -> RuleType`

3. **src/x402guard_game/errors.py**: Exception hierarchy:
   - `X402GuardError(Exception)` with `message: str`, `status_code: int | None`
   - `GuardrailViolationError(X402GuardError)` as frozen dataclass with `rule_type: str`, `limit: int | str`, `actual: int | str`. Include `@classmethod from_response(cls, body: dict) -> GuardrailViolationError` that parses the proxy's 403 error string format "MaxSpendPerTx: limit=1000000 actual=2000000".
   - `ProxyUnreachableError(X402GuardError)`
   - `SessionKeyExpiredError(X402GuardError)`
   - `RateLimitedError(X402GuardError)` with `retry_after: int`

4. **src/x402guard_game/client.py**: Implement `X402GuardClient`:
   - Constructor takes `X402GuardConfig` or keyword args. Falls back to env vars: `X402GUARD_PROXY_URL`, `X402GUARD_AGENT_ID`, `X402GUARD_LOG_LEVEL`. Validates proxy_url is not empty.
   - Uses `httpx.AsyncClient` for all HTTP calls (context manager pattern: `async with X402GuardClient.create(config) as client:`)
   - Also provide sync interface via `X402GuardClient(config)` with sync `httpx.Client` for simpler GAME SDK integration (GAME functions are sync)
   - Private `_request(method, path, body=None) -> dict` handles JSON, content-type, retry, error parsing
   - `_retry_request(method, path, body, attempt=0)`: On 429 read `Retry-After` header, wait with exponential backoff, retry. On network error (`httpx.ConnectError`): retry. On other errors: raise immediately. Max retries from config. Log retries via stdlib `logging` (not print -- project rule).
   - Methods (sync):
     - `health_check() -> bool` -- GET /health, returns True on 200, raises ProxyUnreachableError with "Proxy unreachable at {url} -- is docker compose up running?" on failure
     - `create_agent(req: CreateAgentRequest) -> Agent` -- POST /api/v1/agents
     - `get_agent(agent_id: str) -> Agent` -- GET /api/v1/agents/:id
     - `create_rule(agent_id: str, rule: CreateRuleRequest) -> GuardrailRule` -- POST /api/v1/agents/:id/rules
     - `list_rules(agent_id: str) -> list[GuardrailRule]` -- GET /api/v1/agents/:id/rules
     - `create_session_key(agent_id: str, ...) -> SessionKey` -- POST /api/v1/agents/:id/session-keys
     - `revoke_all(agent_id: str, req: RevokeAllRequest) -> None` -- POST /api/v1/agents/:id/revoke-all
     - `proxy_payment(req: ProxyRequest) -> ProxyResponse` -- POST /api/v1/proxy
     - `proxy_solana_payment(req: SolanaProxyRequest) -> SolanaProxyResponse` -- POST /api/v1/proxy/solana
     - `get_vault_status(owner_pubkey: str) -> dict` -- GET /api/v1/solana/vault/:owner
   - All methods: parse response dict, on 403 raise GuardrailViolationError.from_response, on other non-2xx raise X402GuardError
   - Use stdlib `logging` module (not print). Logger name: `x402guard`.

5. **src/x402guard_game/__init__.py**: Public exports -- X402GuardClient, all types, all errors, helper factory functions.

6. **tests/__init__.py** and **tests/unit/__init__.py**: Empty init files.

7. **tests/unit/test_client.py** (pytest + pytest-httpx):
   - Test: health_check returns True on 200
   - Test: health_check raises ProxyUnreachableError on connection error (message includes URL)
   - Test: create_agent returns typed Agent
   - Test: proxy_payment returns typed ProxyResponse on success
   - Test: proxy_payment raises GuardrailViolationError on 403 with parsed rule_type/limit/actual
   - Test: _request raises X402GuardError on 500
   - Test: constructor raises on empty proxy_url
   - Use `pytest-httpx` to mock httpx responses (`httpx_mock` fixture)

8. **tests/unit/test_errors.py**:
   - Test: GuardrailViolationError.from_response parses "MaxSpendPerTx: limit=1000000 actual=2000000"
   - Test: GuardrailViolationError has correct status_code (403)
   - Test: X402GuardError is instance of Exception

9. **tests/integration/__init__.py**: Empty init file.

10. **tests/integration/test_proxy.py** (pytest):
   Integration tests that run against a live x402Guard proxy (docker compose required).
   These tests are gated by an environment variable `X402GUARD_INTEGRATION=1` so they are skipped by default in unit test runs.
   - Use `@pytest.mark.skipunless(os.getenv("X402GUARD_INTEGRATION"), "requires running proxy")` decorator on the class or module
   - Setup: create X402GuardClient pointing at `os.getenv("X402GUARD_PROXY_URL", "http://localhost:3402")`
   - Test: health_check returns True against running proxy
   - Test: create_agent + get_agent round-trip returns matching agent data
   - Test: create_rule + list_rules round-trip returns the created rule
   - Test: proxy_payment with over-limit amount raises GuardrailViolationError (requires agent + rule setup in fixture)
   - Cleanup: best-effort cleanup of created test agents (don't fail on cleanup errors)

No `print()` statements anywhere in src/ (project rule). Use stdlib `logging` module.
  </action>
  <verify>
    <automated>cd examples/virtuals && pip install -e ".[dev]" && pytest tests/unit/ -x -v</automated>
  </verify>
  <done>Python SDK compiles, all types are frozen dataclasses, error hierarchy parses 403 violations, X402GuardClient wraps all proxy endpoints, 10+ unit tests pass, integration test file exists and is skipped without X402GUARD_INTEGRATION=1</done>
</task>

<task type="auto">
  <name>Task 2: Create GAME SDK function wrappers, demo scripts, and README</name>
  <files>
    examples/virtuals/src/x402guard_game/functions.py
    examples/virtuals/tests/unit/test_functions.py
    examples/virtuals/demo/demo_worker.py
    examples/virtuals/demo/demo_agent.py
    examples/virtuals/README.md
  </files>
  <action>
1. **src/x402guard_game/functions.py**: GAME SDK function wrappers:
   - Import from `game_sdk.game.custom_types`: `Function`, `Argument`, `FunctionResultStatus`
   - Import from `game_sdk.game.agent`: `WorkerConfig`

   **Executable functions** (match GAME SDK signature: `(args...) -> Tuple[FunctionResultStatus, str, dict]`):

   - `make_guarded_payment(target_url: str, amount: str, agent_id: str, network: str = "base-sepolia") -> tuple[FunctionResultStatus, str, dict]`:
     Creates X402GuardClient from env vars. If network starts with "solana": calls proxy_solana_payment. Else: constructs ProxyRequest (with placeholder x402 payment/requirements -- the demo provides real ones). Returns (DONE, message, response_data) on success. Catches GuardrailViolationError: returns (FAILED, formatted violation, {rule_type, limit, actual}). Catches X402GuardError: returns (FAILED, error message, {}).

   - `query_solana_vault(owner_pubkey: str) -> tuple[FunctionResultStatus, str, dict]`:
     Creates X402GuardClient from env vars. Calls get_vault_status(owner_pubkey). Returns (DONE, "Vault status retrieved", vault_data) on success.

   **Function definitions** (GAME SDK Function objects):

   - `guarded_payment_fn = Function(fn_name="make_guarded_payment", fn_description="Make an x402 payment guarded by x402Guard rules. Use when a DeFi transaction is needed.", args=[Argument(name="target_url", description="Target API URL requiring x402 payment"), Argument(name="amount", description="Amount in USDC minor units (1 USDC = 1000000)"), Argument(name="agent_id", description="x402Guard agent ID"), Argument(name="network", description="Network: 'base-sepolia', 'base-mainnet', or 'solana-devnet'")], executable=make_guarded_payment)`

   - `solana_vault_fn = Function(fn_name="query_solana_vault", fn_description="Query Solana vault status for a wallet owner.", args=[Argument(name="owner_pubkey", description="Solana wallet public key of the vault owner")], executable=query_solana_vault)`

   **WorkerConfig** (pre-built worker for easy registration):

   - `defi_worker = WorkerConfig(id="x402guard_defi_worker", worker_description="Handles guarded DeFi payments via x402Guard proxy", get_state_fn=lambda fn_name, fn_args: {"proxy_url": os.getenv("X402GUARD_PROXY_URL", "")}, action_space=[guarded_payment_fn, solana_vault_fn])`

2. **tests/unit/test_functions.py** (pytest):
   - Mock httpx responses to simulate proxy
   - Test: make_guarded_payment returns (DONE, ...) on proxy 200
   - Test: make_guarded_payment returns (FAILED, ...) on guardrail 403 with violation details
   - Test: query_solana_vault returns (DONE, ...) on proxy 200
   - Test: guarded_payment_fn is a valid Function with correct args
   - Test: defi_worker has both functions in action_space

3. **demo/demo_worker.py**: Worker-only demo (NO VIRTUALS_API_KEY needed):
   - Header: "x402Guard + Virtuals GAME SDK Demo (Worker Mode)"
   - Does NOT create Agent -- calls GameFunction executables directly
   - Step 1: Health check (X402GuardClient.health_check())
   - Step 2: Register agent (client.create_agent)
   - Step 3: Set guardrail (client.create_rule with max_spend_per_tx(1_000_000))
   - Step 4: Call make_guarded_payment directly with amount under limit -- should return DONE
   - Step 5: Call make_guarded_payment with amount over limit (2_000_000) -- should return FAILED with formatted violation:
     ```
     [BLOCKED] GuardrailViolationError: MaxSpendPerTx limit=1000000 actual=2000000
       rule_type: MaxSpendPerTx
       limit:     1,000,000 (1.00 USDC)
       actual:    2,000,000 (2.00 USDC)
     ```
   - All config from env vars: X402GUARD_PROXY_URL (default http://localhost:3402)
   - Uses stdlib logging for all output. Exception: the formatted violation display uses print for readability (per user decision -- demo scripts are the only place print is acceptable)
   - Construct demo EVM payment payload: base64url-encoded JSON with scheme "exact", network "base-sepolia", and a placeholder signature. The proxy will validate the signature format but since this is off-chain only, construct a structurally valid but cryptographically meaningless payload (the proxy will reject it on signature verification, so the demo focuses on guardrail violations by sending an over-limit amount first).
   - ALTERNATIVE: If the proxy rejects invalid signatures before checking guardrails, use a pre-step to demonstrate the SDK API calls (register, create rule, list rules) and skip the actual proxy payment call. Document this limitation in README.
   - Exit 0 on success, exit 1 on unexpected error

4. **demo/demo_agent.py**: Full agentic demo (REQUIRES VIRTUALS_API_KEY):
   - Header: "x402Guard + Virtuals GAME SDK Demo (Agent Mode)"
   - Requires VIRTUALS_API_KEY env var -- check at startup, fail with clear message
   - Creates Agent with defi_worker
   - Runs agent.run() for a limited number of steps
   - Documents that the GAME LLM planner will decide when to call make_guarded_payment
   - SECURITY: Do NOT hardcode any API keys. All from env vars.

5. **README.md**: Comprehensive documentation:
   - Title: `x402guard-game-plugin`
   - Overview: What x402Guard is, what this plugin does for Virtuals Protocol GAME agents
   - Prerequisites: Python 3.11+, Docker (for proxy), pip
   - Quick Start: 5 steps (clone, install, docker compose up, set env vars, run demo)
   - Architecture diagram (Mermaid):
     ```mermaid
     graph LR
       A[GAME Agent/Worker] --> B[x402guard_game functions]
       B --> C[X402GuardClient]
       C --> D[x402Guard Proxy :3402]
       D --> E[Target API]
       D --> F[(PostgreSQL)]
       D --> G[(Redis)]
     ```
   - Two Demo Modes:
     - Worker mode: `python demo/demo_worker.py` (no API key needed)
     - Agent mode: `python demo/demo_agent.py` (requires VIRTUALS_API_KEY from game.virtuals.io)
   - Configuration Reference: Table of env vars (X402GUARD_PROXY_URL, X402GUARD_AGENT_ID, X402GUARD_LOG_LEVEL, VIRTUALS_API_KEY) with defaults
   - SDK API Reference: X402GuardClient methods, GameFunction factories
   - GAME SDK Integration: How to register defi_worker in your own Agent
   - Error Handling: GuardrailViolationError, ProxyUnreachableError, RateLimitedError
   - Troubleshooting: Common issues (proxy unreachable, missing API key, wrong Python version)
   - Development: How to install dev deps, run tests, run mypy
   - License: MIT
  </action>
  <verify>
    <automated>cd examples/virtuals && pytest tests/unit/ -x -v</automated>
  </verify>
  <done>GAME SDK functions wrap proxy calls correctly, demo_worker.py compiles and shows the full flow, demo_agent.py compiles with API key check, README covers both demo modes with architecture diagram, 5+ function tests pass</done>
</task>

</tasks>

<verification>
1. `cd examples/virtuals && pip install -e ".[dev]"` -- package installs successfully
2. `cd examples/virtuals && pytest tests/unit/ -x -v` -- all unit tests pass
3. `cd examples/virtuals && mypy src/x402guard_game/` -- type checks pass (may need to skip game_sdk stubs)
4. Verify types.py uses frozen dataclasses for ALL types (project immutability rule)
5. Verify no print() statements in src/ (only in demo/ scripts)
6. Verify RuleType format matches Rust serde: {"type": "MaxSpendPerTx", "params": {"limit": 1000000}}
7. Verify README.md contains Mermaid architecture diagram and both demo modes
8. Verify integration test file exists at tests/integration/test_proxy.py and is gated by X402GUARD_INTEGRATION env var
</verification>

<success_criteria>
- x402guard-game-plugin package installs and imports cleanly
- X402GuardClient wraps all proxy endpoints with typed frozen dataclasses
- Error hierarchy parses 403 guardrail violations with structured fields
- GameFunction wrappers conform to GAME SDK Function/Argument/FunctionResultStatus interface
- WorkerConfig pre-built with both payment and vault functions
- demo_worker.py runs without VIRTUALS_API_KEY (direct function calls)
- demo_agent.py requires VIRTUALS_API_KEY with clear error on missing
- 15+ unit tests pass covering client, errors, and functions
- README covers prerequisites, two demo modes, config reference, architecture diagram, troubleshooting
- No print() in production code, no hardcoded secrets
- Integration tests exist and are gated by X402GUARD_INTEGRATION=1
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-examples/05-03-SUMMARY.md`
</output>
